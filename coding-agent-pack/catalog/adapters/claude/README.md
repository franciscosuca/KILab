# Claude Code Adapter

The installer renders reusable agents from `catalog/core/agents/` into Claude Code frontmatter and turns reusable skills into Claude commands under `.claude/commands/` or `~/.claude/commands/`.

`CLAUDE.md` and `implementation-plan.md` are Claude-specific adapter content. Legacy wrappers and project-specific commands are preserved under `catalog/archived/claude/`.
