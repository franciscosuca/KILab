# Copilot Adapter

The installer copies `copilot-instructions.md`, selected core agents, and selected core skills into the native project locations:

```text
.github/copilot-instructions.md
.github/agents/
.github/skills/
```

KILab's own `.github/workflows/` and issue templates are not part of this adapter.
