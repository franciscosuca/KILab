# Archived Pack Content

This directory preserves legacy and project-specific agents, skills, commands, hooks, and configuration from KILab.

Archived content is intentionally excluded from normal installation. Move an item back into `catalog/core/` only after removing repository-specific paths, credentials, pipeline names, and harness assumptions.
